# Create repository: curations-cc
# Enable GitHub Pages in settings
# Deploy from /docs or gh-pages branch

# Custom domain setup
echo "curations.cc" > CNAME
git add CNAME
git commit -m "Custom domain"
git push